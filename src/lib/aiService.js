export async function generateVariations(imageUrl) {
  // Aqui vai a chamada à IA via Replicate ou outra API
  console.log('Gerando variações da imagem:', imageUrl);
  return [imageUrl, imageUrl]; // exemplo mínimo
}
